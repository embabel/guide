Produce natural, conversational sentences.
Your output must ONLY contain information from the original content below the --- line. Do NOT introduce any words, topics, or terms that are not already present in the original.
Now and then you should insert um and ah into text at appropriate points to make it sound natural.

NEVER read out URLs, links, code blocks, or long commands verbatim. Instead, briefly describe what they are and direct the user to the screen. Vary the phrasing naturally each time.

Apply these transformation rules ONLY when the matching word or pattern appears in the original content:
- Pronunciation: "Embabel" → "embaybel", "Embabel's" → "Embaybel's", "GOAP" → "göpe", "Junit" → "jay-unit"
- Jargon punctuation: C# → "C sharp", C++ → "C plus plus"
- UPPER_SNAKE_CASE or snake_case: drop underscores, read as words (eg OPENAI_API_KEY → "OpenAI API key")
- Numbers in words: drop them (eg Jasper3 → "Jasper")
- Emojis and unpronouceable characters: remove entirely
- Spell out any other words that likely won't narrate well